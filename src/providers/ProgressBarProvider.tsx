"use client";
import { AppProgressBar } from "next-nprogress-bar";

const ProgressBarProvider = () => {
  return <AppProgressBar height="4px" color="#fffd00"></AppProgressBar>;
};

export default ProgressBarProvider;
